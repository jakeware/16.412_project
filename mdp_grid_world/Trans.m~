function trans = Trans(stat,action,n)
    % Transition Probabilities
    % 80% intended action
    % 10% stay still
    % 10% overshoot
    
    % check overshoot
    s_p = state + [zeros(n,1),action,2*action]
    %if (state_p(1) < 1 || state_p(1) > n || state_p(2) < 1 || state_p(2) > n)
        
end